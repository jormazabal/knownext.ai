[package]
name = "knownext-research"
version = "2.5.1"
edition = "2021"
license = "Apache-2.0"

[dependencies]
knownext-core = { path = "../knownext-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
