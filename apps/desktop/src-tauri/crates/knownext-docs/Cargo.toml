[package]
name = "knownext-docs"
version = "2.0.2"
edition = "2021"
license = "Apache-2.0"

[dependencies]
knownext-core = { path = "../knownext-core" }
serde_json = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
