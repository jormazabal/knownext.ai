[package]
name = "knownext-ai"
version = "2.4.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
base64 = "0.22"
knownext-core = { path = "../knownext-core" }
knownext-ai-skills = { path = "../knownext-ai-skills" }
reqwest = { version = "0.13", default-features = false, features = ["blocking", "json", "multipart", "rustls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
